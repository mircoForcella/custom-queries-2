spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devdb
    username: developer
    password: password
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
